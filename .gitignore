node_modules/
.expo/
.expo-shared/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
*.apk
*.aab
.env
# NOTE: the old gitignored root config.ts is gone. App-wide public constants
# live in the committed src/constants/index.ts — a mobile bundle is public, so
# hiding them bought nothing and broke a clean checkout. Secrets stay on the
# server, which is why this app has no environment file at all.

# macOS
.DS_Store

